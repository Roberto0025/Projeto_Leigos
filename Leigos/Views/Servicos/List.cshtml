@model ServicosListViewModel

<div>
    <h2>Todos os serviços</h2>
</div>

<div class="container">
    <div class="row">
        @foreach (var serv in Model.Servicos)
        {
            <partial name="_Resumo" for="@serv"/>
        }
    </div>
</div>